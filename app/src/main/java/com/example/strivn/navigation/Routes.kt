package com.example.strivn.navigation

/**
 * Central place for navigation route constants.
 * Keep routes stable to avoid breaking deep links and saved state.
 */
object Routes {
    const val AUTH_LOGIN = "auth_login"
    const val AUTH_ONBOARDING = "auth_onboarding"

    const val HOME = "home"
    const val PROGRESS = "progress"
    const val CHECK_IN = "check_in"
    const val SIMULATION = "simulation"
    const val ADAPT_AI = "adapt_ai"
    const val PAST_RUNS = "past_runs"
}

