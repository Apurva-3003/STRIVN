package com.example.strivn.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * STRIVN Dark UI color system.
 *
 * Keep these as reusable constants and reference them from the Material3 ColorScheme and
 * component defaults.
 */

// Backgrounds / surfaces
val StrivnBackground = Color(0xFF0F172A)
val StrivnPrimaryCard = Color(0xFF151E35)
val StrivnSecondaryCard = Color(0xFF1B2542)

// Actions
val StrivnAccent = Color(0xFF14B8A6) // Primary action
val StrivnSecondaryAction = Color(0xFF2979FF)
val StrivnWarning = Color(0xFFF59E0B) // yellow/orange for "medium" states
val StrivnError = Color(0xFFF97373)

// Text
val StrivnTextPrimary = Color(0xFFFFFFFF)
val StrivnTextSecondary = Color(0xFFD1D5DB) // light gray